const connection = require("../mysql-synchronous/connection");

// 创建钱包
exports.createWallet = async function createWallet(){

    return new Promise(async function (resolve, reject) {

        resolve();

        // 响应结果
        console.log("into createWallet");
    })
}
